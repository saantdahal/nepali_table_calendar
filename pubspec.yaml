name: nepali_table_calendar
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.18.0
  simple_gesture_detector: ^0.2.0
  nepali_utils: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
